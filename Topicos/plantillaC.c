#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <limits.h> // For INT_MAX, LLONG_MAX, etc.


void solve(){
    // Code
}

int main() {
    // Fast I/O (optional, but can be helpful for large inputs)
    // setvbuf(stdout, NULL, _IOFBF, 1024 * 1024); // Example for buffered output
    // setvbuf(stdin, NULL, _IOFBF, 1024 * 1024); // Example for buffered input

    // Your main problem-solving logic goes here
    // Example: Reading input and printing output
    int test_cases;
    scanf("%d", &test_cases);
    while (test_cases--) {
        solve();
    }

    return 0;
}
